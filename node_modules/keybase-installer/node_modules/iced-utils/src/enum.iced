#==========================================================================

exports.Enum = class Enum
  constructor : (@codes) -> 
    @lookup = {}
    for k,v of @codes
      @lookup[v] = k
      
#==========================================================================
