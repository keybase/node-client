// Generated by IcedCoffeeScript 1.6.3-i
(function() {
  var args_mutate, env, gw, log;

  gw = require('gpg-wrapper');

  env = require('./env').env;

  log = require('./log');

  args_mutate = function(opts) {
    if (opts.tmp) {
      log.debug("| Accessing the temporary keychain");
      opts.args = ["--keyring", env().get_tmp_gpg_pub_keyring(), "--secret-keyring", env().get_tmp_gpg_sec_keyring(), "--trustdb-name", env().get_tmp_gpg_trustdb(), "--no-default-keyring", "--no-random-seed-file"].concat(opts.args);
    }
    log.debug("| Run PGP with opts: " + (JSON.stringify(opts)));
    return gpg(opts, cb);
  };

}).call(this);
