// Generated by IcedCoffeeScript 1.6.3-g
(function() {
  var ArgumentParser, Base, BufferOutStream, Command, E, PackageJson, ProgressBar, Prompter, SC, add_option_dict, checkers, constants, env, gpg, iced, log, make_esc, req, rng, triplesec, __iced_k, __iced_k_noop, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  iced = require('iced-coffee-script/lib/coffee-script/iced').runtime;
  __iced_k = __iced_k_noop = function() {};

  Base = require('./base').Base;

  log = require('../log');

  ArgumentParser = require('argparse').ArgumentParser;

  add_option_dict = require('./argparse').add_option_dict;

  PackageJson = require('../package').PackageJson;

  gpg = require('../gpg').gpg;

  BufferOutStream = require('../stream').BufferOutStream;

  E = require('../err').E;

  Prompter = require('../prompter').Prompter;

  checkers = require('../checkers').checkers;

  make_esc = require('iced-error').make_esc;

  triplesec = require('triplesec');

  rng = require('crypto').rng;

  constants = require('../constants').constants;

  SC = constants.security;

  ProgressBar = require('progress');

  req = require('../req');

  env = require('../env').env;

  exports.Command = Command = (function(_super) {
    __extends(Command, _super);

    function Command() {
      _ref = Command.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Command.prototype.OPTS = {
      e: {
        aliases: ['email'],
        help: 'the email address to signup'
      },
      u: {
        aliase: ['username'],
        help: 'the username to signup as'
      }
    };

    Command.prototype.add_subcommand_parser = function(scp) {
      var name, opts, sub;
      opts = {
        aliases: ["signup"]
      };
      name = "join";
      sub = scp.addParser(name, opts);
      return opts.aliases.concat([name]);
    };

    Command.prototype.prompt = function(cb) {
      var e, err, p, seq, u, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      seq = {
        username: {
          prompt: "Your desired username",
          checker: checkers.username
        },
        passphrase: {
          prompt: "Your passphrase",
          passphrase: true,
          checker: checkers.passphrase,
          confirm: {
            prompt: "confirm passphrase"
          }
        },
        email: {
          prompt: "Your email",
          checker: checkers.email
        },
        invite: {
          prompt: "Invitation code",
          checker: checkers.invite_code,
          defval: "123412341234123412341234"
        }
      };
      if (!this.prompter) {
        if ((u = env().get_username()) != null) {
          seq.username.defval = u;
        }
        if ((p = env().get_passphrase()) != null) {
          seq.passphrase.defval = p;
        }
        if ((e = env().get_email()) != null) {
          seq.email.defval = e;
        }
        this.prompter = new Prompter(seq);
      }
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.prompt"
        });
        _this.prompter.run(__iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              return err = arguments[0];
            };
          })(),
          lineno: 69
        }));
        __iced_deferrals._fulfill();
      })(function() {
        if (typeof err === "undefined" || err === null) {
          _this.data = _this.prompter.data();
        }
        return cb(err);
      });
    };

    Command.prototype.gen_pwh = function(cb) {
      var bar, err, extra_keymaterial, km, prev, progress_hook, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      (function(__iced_k) {
        if (!_this.pw_last || (_this.pw_last !== _this.data.passphrase)) {
          _this.enc = new triplesec.Encryptor({
            key: new Buffer(_this.data.passphrase, 'utf8'),
            verion: SC.triplesec.version
          });
          _this.pw_last = _this.data.passphrase;
          bar = null;
          prev = 0;
          progress_hook = function(obj) {
            if (obj.what !== "scrypt") {

            } else {
              bar || (bar = new ProgressBar("Scrypt [:bar] :percent", {
                width: 35,
                total: obj.total
              }));
              bar.tick(obj.i - prev);
              return prev = obj.i;
            }
          };
          extra_keymaterial = SC.pwh.derived_key_bytes + SC.openpgp.derived_key_bytes;
          (function(__iced_k) {
            __iced_deferrals = new iced.Deferrals(__iced_k, {
              parent: ___iced_passed_deferral,
              filename: "src/command/join.iced",
              funcname: "Command.gen_pwh"
            });
            _this.enc.resalt({
              extra_keymaterial: extra_keymaterial,
              progress_hook: progress_hook
            }, __iced_deferrals.defer({
              assign_fn: (function() {
                return function() {
                  err = arguments[0];
                  return km = arguments[1];
                };
              })(),
              lineno: 97
            }));
            __iced_deferrals._fulfill();
          })(function() {
            return __iced_k(typeof err === "undefined" || err === null ? (_this.salt = _this.enc.salt.to_hex(), _this.pwh = km.extra.slice(0, SC.pwh.derived_key_bytes).toString('hex')) : void 0);
          });
        } else {
          return __iced_k();
        }
      })(function() {
        return cb(err);
      });
    };

    Command.prototype.post = function(cb) {
      var args, body, err, retry, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      args = {
        salt: this.salt,
        pwh: this.pwh,
        username: this.data.username,
        email: this.data.email,
        invitation_id: this.data.invite
      };
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.post"
        });
        req.post({
          endpoint: "signup",
          args: args
        }, __iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              err = arguments[0];
              return body = arguments[1];
            };
          })(),
          lineno: 114
        }));
        __iced_deferrals._fulfill();
      })(function() {
        retry = false;
        if ((typeof err !== "undefined" && err !== null) && (err instanceof E.KeybaseError)) {
          switch (body.status.name) {
            case 'BAD_SIGNUP_EMAIL_TAKEN':
              log.error("Email address '" + _this.data.email + "' already registered");
              retry = true;
              _this.prompter.clear('email');
              err = null;
              break;
            case 'BAD_SIGNUP_USERNAME_TAKEN':
              log.error("Username '" + _this.data.username + "' already registered");
              retry = true;
              _this.prompter.clear('username');
              err = null;
          }
        }
        if (err == null) {
          _this.uid = body.uid;
        } else if (!retry) {
          log.error("Unexpected error: " + err);
        }
        return cb(err, retry);
      });
    };

    Command.prototype.write_out = function(cb) {
      var c, err, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      c = env().config;
      c.set("user.email", this.data.email);
      c.set("user.salt", this.salt);
      c.set("user.name", this.data.username);
      c.set("user.id", this.uid);
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.write_out"
        });
        c.write(__iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              return err = arguments[0];
            };
          })(),
          lineno: 142
        }));
        __iced_deferrals._fulfill();
      })(function() {
        return cb(err);
      });
    };

    Command.prototype.run = function(cb) {
      var esc, retry, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      esc = make_esc(cb, "Join::run");
      retry = true;
      (function(__iced_k) {
        var _results, _while;
        _results = [];
        _while = function(__iced_k) {
          var _break, _continue, _next;
          _break = function() {
            return __iced_k(_results);
          };
          _continue = function() {
            return iced.trampoline(function() {
              return _while(__iced_k);
            });
          };
          _next = function(__iced_next_arg) {
            _results.push(__iced_next_arg);
            return _continue();
          };
          if (!retry) {
            return _break();
          } else {
            (function(__iced_k) {
              __iced_deferrals = new iced.Deferrals(__iced_k, {
                parent: ___iced_passed_deferral,
                filename: "src/command/join.iced",
                funcname: "Command.run"
              });
              _this.prompt(esc(__iced_deferrals.defer({
                lineno: 151
              })));
              __iced_deferrals._fulfill();
            })(function() {
              (function(__iced_k) {
                __iced_deferrals = new iced.Deferrals(__iced_k, {
                  parent: ___iced_passed_deferral,
                  filename: "src/command/join.iced",
                  funcname: "Command.run"
                });
                _this.gen_pwh(esc(__iced_deferrals.defer({
                  lineno: 152
                })));
                __iced_deferrals._fulfill();
              })(function() {
                (function(__iced_k) {
                  __iced_deferrals = new iced.Deferrals(__iced_k, {
                    parent: ___iced_passed_deferral,
                    filename: "src/command/join.iced",
                    funcname: "Command.run"
                  });
                  _this.post(esc(__iced_deferrals.defer({
                    assign_fn: (function() {
                      return function() {
                        return retry = arguments[0];
                      };
                    })(),
                    lineno: 153
                  })));
                  __iced_deferrals._fulfill();
                })(_next);
              });
            });
          }
        };
        _while(__iced_k);
      })(function() {
        (function(__iced_k) {
          __iced_deferrals = new iced.Deferrals(__iced_k, {
            parent: ___iced_passed_deferral,
            filename: "src/command/join.iced",
            funcname: "Command.run"
          });
          _this.write_out(esc(__iced_deferrals.defer({
            lineno: 154
          })));
          __iced_deferrals._fulfill();
        })(function() {
          return cb(null);
        });
      });
    };

    return Command;

  })(Base);

}).call(this);
