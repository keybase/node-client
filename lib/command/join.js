// Generated by IcedCoffeeScript 1.6.3-g
(function() {
  var ArgumentParser, Base, BufferOutStream, Command, E, PackageJson, ProgressBar, Prompter, SC, add_option_dict, checkers, constants, gpg, iced, log, make_esc, rng, triplesec, __iced_k, __iced_k_noop, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  iced = require('iced-coffee-script/lib/coffee-script/iced').runtime;
  __iced_k = __iced_k_noop = function() {};

  Base = require('./base').Base;

  log = require('../log');

  ArgumentParser = require('argparse').ArgumentParser;

  add_option_dict = require('./argparse').add_option_dict;

  PackageJson = require('../package').PackageJson;

  gpg = require('../gpg').gpg;

  BufferOutStream = require('../stream').BufferOutStream;

  E = require('../err').E;

  Prompter = require('../prompter').Prompter;

  checkers = require('../checkers').checkers;

  make_esc = require('iced-error').make_esc;

  triplesec = require('triplesec');

  rng = require('crypto').rng;

  constants = require('../constants').constants;

  SC = constants.security;

  ProgressBar = require('progress');

  exports.Command = Command = (function(_super) {
    __extends(Command, _super);

    function Command() {
      _ref = Command.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Command.prototype.add_subcommand_parser = function(scp) {
      var name, opts, sub;
      opts = {
        aliases: ["signup"]
      };
      name = "join";
      sub = scp.addParser(name, opts);
      return opts.aliases.concat([name]);
    };

    Command.prototype.prompt = function(cb) {
      var err, p, seq, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      seq = {
        username: {
          prompt: "Your desired username",
          checker: checkers.username
        },
        passphrase: {
          prompt: "Your passphrase",
          passphrase: true,
          checker: checkers.passphrase,
          confirm: {
            prompt: "confirm passphrase"
          }
        },
        email: {
          prompt: "Your email",
          checker: checkers.email
        },
        invite: {
          prompt: "Invitation code",
          checker: checkers.invite_code
        }
      };
      p = new Prompter(seq);
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.prompt"
        });
        p.run(__iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              return err = arguments[0];
            };
          })(),
          lineno: 51
        }));
        __iced_deferrals._fulfill();
      })(function() {
        if (typeof err === "undefined" || err === null) {
          _this.data = p.data();
        }
        return cb(err);
      });
    };

    Command.prototype.gen_pwh = function(cb) {
      var bar, err, extra_keymaterial, km, prev, progress_hook, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      console.log(this.data);
      this.enc = new triplesec.Encryptor({
        key: new Buffer(this.data.passphrase, 'utf8'),
        verion: SC.triplesec.version
      });
      bar = null;
      prev = 0;
      progress_hook = function(obj) {
        if (obj.what !== "scrypt") {

        } else {
          bar || (bar = new ProgressBar("Scrypt [:bar] :percent", {
            width: 35,
            total: obj.total
          }));
          bar.tick(obj.i - prev);
          return prev = obj.i;
        }
      };
      extra_keymaterial = SC.pwh.derived_key_bytes + SC.openpgp.derived_key_bytes;
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.gen_pwh"
        });
        _this.enc.resalt({
          extra_keymaterial: extra_keymaterial,
          progress_hook: progress_hook
        }, __iced_deferrals.defer({
          assign_fn: (function() {
            return function() {
              err = arguments[0];
              return km = arguments[1];
            };
          })(),
          lineno: 74
        }));
        __iced_deferrals._fulfill();
      })(function() {
        if (typeof err === "undefined" || err === null) {
          _this.salt = _this.enc.salt;
          _this.pwh = km.extra.slice(0, SC.pwh.derived_key_bytes);
          console.log(_this.salt);
          console.log(_this.pwh);
        }
        return cb(err);
      });
    };

    Command.prototype.run = function(cb) {
      var esc, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      esc = make_esc(cb, "Join::run");
      (function(__iced_k) {
        __iced_deferrals = new iced.Deferrals(__iced_k, {
          parent: ___iced_passed_deferral,
          filename: "src/command/join.iced",
          funcname: "Command.run"
        });
        _this.prompt(esc(__iced_deferrals.defer({
          lineno: 86
        })));
        __iced_deferrals._fulfill();
      })(function() {
        (function(__iced_k) {
          __iced_deferrals = new iced.Deferrals(__iced_k, {
            parent: ___iced_passed_deferral,
            filename: "src/command/join.iced",
            funcname: "Command.run"
          });
          _this.gen_pwh(esc(__iced_deferrals.defer({
            lineno: 87
          })));
          __iced_deferrals._fulfill();
        })(function() {
          console.log(_this.data);
          return cb(null);
        });
      });
    };

    return Command;

  })(Base);

}).call(this);
