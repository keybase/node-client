// Generated by IcedCoffeeScript 1.6.3-g
(function() {
  var ArgumentParser, Base, BufferOutStream, Command, E, PackageJson, add_option_dict, env, gpg, iced, log, __iced_k, __iced_k_noop, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  iced = require('iced-coffee-script/lib/coffee-script/iced').runtime;
  __iced_k = __iced_k_noop = function() {};

  Base = require('./base').Base;

  log = require('../log');

  ArgumentParser = require('argparse').ArgumentParser;

  add_option_dict = require('./argparse').add_option_dict;

  PackageJson = require('../package').PackageJson;

  gpg = require('../gpg').gpg;

  BufferOutStream = require('../stream').BufferOutStream;

  E = require('../err').E;

  env = require('../env').env;

  exports.Command = Command = (function(_super) {
    __extends(Command, _super);

    function Command() {
      _ref = Command.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Command.prototype.OPTS = {
      get: {
        help: "Read the given configuration option"
      }
    };

    Command.prototype.add_subcommand_parser = function(scp) {
      var name, opts, sub;
      opts = {
        aliases: ["conf"],
        help: "make an initial configuration file"
      };
      name = "config";
      sub = scp.addParser(name, opts);
      add_option_dict(sub, this.OPTS);
      sub.addArgument(["kvs"], {
        nargs: "*"
      });
      return opts.aliases.concat([name]);
    };

    Command.prototype.config_opts = function() {
      return {
        in_config: true
      };
    };

    Command.prototype.run = function(cb) {
      var c, err, k, msg, pjs, v, ___iced_passed_deferral, __iced_deferrals, __iced_k,
        _this = this;
      __iced_k = __iced_k_noop;
      ___iced_passed_deferral = iced.findDeferral(arguments);
      err = null;
      c = env().config;
      if ((k = this.argv.get) != null) {
        console.log(c.get(k));
      } else if (this.argv.kvs.length > 2) {
        msg = "Need either 0,1 or 2 arguments for setting values in config";
        log.error("Usage: " + msg);
        err = new E.ArgsError(msg);
      } else if (this.argv.kvs.length > 0) {
        k = this.argv.kvs[0];
        v = this.argv.kvs.length === 2 ? this.argv.kvs[1] : null;
        c.set(k, v);
      } else if (c.is_empty()) {
        pjs = new PackageJson();
        c.set("generated", {
          by: "keybase v" + (pjs.version()),
          on: (new Date()).toString()
        });
      } else {
        log.warn("keybase has already been initialized; see '" + c.filename + "'");
      }
      (function(__iced_k) {
        if (!err && c.is_dirty()) {
          (function(__iced_k) {
            __iced_deferrals = new iced.Deferrals(__iced_k, {
              parent: ___iced_passed_deferral,
              filename: "src/command/config.iced",
              funcname: "Command.run"
            });
            c.write(__iced_deferrals.defer({
              assign_fn: (function() {
                return function() {
                  return err = arguments[0];
                };
              })(),
              lineno: 60
            }));
            __iced_deferrals._fulfill();
          })(__iced_k);
        } else {
          return __iced_k();
        }
      })(function() {
        return cb(err);
      });
    };

    return Command;

  })(Base);

}).call(this);
